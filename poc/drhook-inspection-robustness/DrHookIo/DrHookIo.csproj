<Project Sdk="Microsoft.NET.Sdk">

  <!-- DrHook isolation probe, round 3 (ADR-007). Common + unsafe-pointer shapes were all
       safe at depth 2. This tests framework IO/native-handle objects (FileStream's SafeHandle;
       MemoryMappedViewAccessor's SafeMemoryMappedViewHandle) — QuadStore's actual mmap field
       shape — which depth-2 walks INTO. -->
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <DebugType>portable</DebugType>
    <Optimize>false</Optimize>
  </PropertyGroup>

</Project>
